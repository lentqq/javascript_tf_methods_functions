function addAndSubtract (first,second,last){

    let subtractResult = sum0fTwoNumbers(first, second) - last;
    console.log(subtractResult);
}

addAndSubtract(1,17,30)

function sum0fTwoNumbers (first,second){
    let sum = first + second;
    return sum;
}